<h2><%= @list.name %></h2>

<ul class="list-group">
    <% @list.bookmarks.each do |bookmark| %>
      <li class="list-group-item"><strong>Content:</strong> <%= bookmark.comment %></li>
    <% end %>
</ul>

<%= link_to 'Add a movie', new_list_bookmark_path(@list) %>

<%= link_to 'Homepage', lists_path %>
